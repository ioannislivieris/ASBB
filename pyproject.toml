[project]
name = "asbb"
version = "0.1.0"
description = ""
authors = [
    { name = "livieris@upatras.gr" }
]
license = { text = "MIT" }
readme = "README.md"

# 🔧 FIXED
requires-python = ">=3.11,<3.15"

dependencies = [
    "torch>=2.10.0,<3.0.0",
    "numpy>=2.4.2,<3.0.0",
    "scikit-learn>=1.8.0,<2.0.0"
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
